export interface ModalProps{
    openModal: boolean,
    handleClose: any,
    name: string,
    launchDate: string,
    rocketID: string,
    launchpadID: string,
    success: boolean,
    details: string
}